<%= form_for(@post) do |f| %>
  <div class="meme-form">
    <%= f.hidden_field :type, value: 'meme' %>
    <!-- FIXME -->
  </div>
  <div class="actions">
    <%= f.submit "Submit Meme", class: "submit-button btn btn-primary btn-large" %>
  </div>
<% end %>
