<p id="notice"><%= notice %></p>

<div class="post">
  <div class="content span6 well">
    <%= @post.content %>
  </div>
  <div class="meta timestamp span2">
    <%= link_to @post.created_at.strftime("%I:%M%p on %m/%d/%Y"), post_path(@post) %>
  </div>
  <div class="vote span2">
    <a class="like btn btn-success">
      <span class="count"><%= @post.likes %></span>
      <i class="icon-thumbs-up"></i>
    </a>
    <a class="dislike btn btn-danger">
      <span class="count"><%= @post.dislikes %></span>
      <i class="icon-thumbs-down"></i>
    </a>
  </div>
  <div class="span2 meta">
    <%= link_to pluralize(@post.comments.count, 'Comment'), post_path(@post) + '#new_comment' %>
  </div>

  <ul class="comments span6">
    <% unless @post.comments.empty? %>
      <%= render partial: 'comments/comment', collection: @post.comments %>
    <% end %>
    <%= render 'comments/form' %>
    <%= link_to 'Back', root_url %>
  </ul>
</div>



